import PostsPage from "../../components/PostsPage";

export const metadata = {
    title: 'ProjectName | Blog',
}

const Blog = () => {
    return (
        <PostsPage/>
    );
};

export default Blog;
