export const metadata = {
    title: 'ProjectName | Contacts',
}
const Contacts = () => {
    return (
        <>
            Сюда не веде никакаы ссылка, пока контакты расположены на странице информации
        </>
    );
};

export default Contacts;
